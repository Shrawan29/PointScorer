{
  "name": "pointscorer",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "axios": "^1.13.4"
  }
}
